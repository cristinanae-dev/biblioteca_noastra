name: biblioteca_noastra
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  audioplayers: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets - foarte important să fie indentat corect
  assets:
    - assets/carti/crima_pedeapsa/
    - assets/carti/semizeul/
    - assets/carti/mos_goriot/